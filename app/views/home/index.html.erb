<div class="jumbotron text-center">
  <h3>The Universe is the totality of everything that exists, has existed, or even will exist.</h3>
  <p>
    This Q&amp;A site is dedicated to questions about the history of the universe.
  </p>
</div>


<div class="bs-example" data-example-id="default-media">
  <% @questions.each do |q| %>
    <div class="media">
      <div class="media-left">
        <a href="#">
          <img src="<%= q.gravatar %>" />
        </a>
      </div>
      <div class="media-body">
        <h4 class="media-heading"><%= q.email %> asked:</h4>
        <p><%= time_ago_in_words q.created_at %> ago</p>
        <%= q.body %>
        <div><a href="<%= question_path(q) %>" class="btn btn-success btn-xs">View Answers</a></div>
      </div>
    </div>
 <% end %>

  <% if @questions.empty? %>
    <div class="alert alert-info">Be the first to ask a question</div>
  <% end %>
</div>
